import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class Scooby {

	public static void main(String[] args) throws FileNotFoundException {
		Scanner SC = new Scanner(new File("scooby.dat"));
		
		int run = SC.nextInt();
		for (int i = 0; i < run; i++) {
			String paths = SC.nextLine();
			String check = SC.nextLine();
			checkPath(check, paths);
		}
	}
	
	public static void checkPath(String lPaths, String lCheck) {
		String[] Paths = lPaths.split(" ");
		String[] Rooms = new String[Paths.length * 2];
		for (String string : Paths) {
			System.out.println(string);
		}
		System.out.println();
	}

}
